package main

import (
	"testing"
)

func TestControlLoop(t *testing.T) {
	// set up 1 coordinator
	// should start
	// propose should return failure with 0 leader
	// sm := newTestStateMachine(1)
	// rpcConfig :=
	// coordinator := NewCoordinator()

}
